Algoritmo "questao2"
Var
numero1, mult, antecessor:real


Inicio
escreval("insira o numero")
leia(numero1)

antecessor <- (numero1) - (numero1 - 1);
mult <- (numero1) * (numero1+1);

escreval("O antecessor do numero e ", antecessor," e o sucessor multiplicado e",mult)

Fimalgoritmo
